# --- SEGURIDAD ---
# Genera una nueva con: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=UXJig)7j84HH2vzNv3-+6*NSACZek4fGZ6(rTeG_t==gLVfTy6 # pragma: allowlist secret
# Cambiar a False en producción. True solo para desarrollo local.
# Debug debe ser False en producción por seguridad.
DEBUG=False
#allowe hosts debe incluir los dominios permitidos en producción.
#en desarrollo local, localhost y
#la IP local
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
# --- BASE DE DATOS ---
POSTGRES_DB=robotica_db # pragma: allowlist secret
POSTGRES_USER=msathiu# pragma: allowlist secret
POSTGRES_PASSWORD=tu_password_segura # pragma: allowlist secret
# Cadena de conexión a la base de datos PostgreSQL
# Si usas Docker con el servicio llamado 'db', sería:
DATABASE_URL=postgres://msathiu:tu_password_segura@db:5432/robotica_db # pragma: allowlist secret

# --- CONFIGURACIÓN DE EMAIL (Mailtrap para Desarrollo) ---
#En producción, usar un servicio real
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=sandbox.smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=
DEFAULT_FROM_EMAIL=

# --- VARIABLES DEL SITIO ---
#en producción cambiar al dominio real
BASE_URL=http://localhost:

DJANGO_SETTINGS_MODULE=SistemaRegistro.settings
